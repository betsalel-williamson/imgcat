DELIMITER $$
CREATE OR REPLACE FUNCTION Posts.GetPostIdByLink(
	p_link CHAR(12)
)
RETURNS INT UNSIGNED
LANGUAGE SQL
NOT DETERMINISTIC
MODIFIES SQL DATA
SQL SECURITY DEFINER
BEGIN
	RETURN (
		SELECT id
		FROM Posts.Post
		WHERE link = p_link
	);
END
$$
DELIMITER ;
